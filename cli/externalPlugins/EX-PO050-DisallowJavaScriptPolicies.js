/*
  Generated by apigeelint Rule Studio. Apache-2.0.

  This is an apigeelint plugin (a custom lint rule). It is loaded automatically
  by the bundled CLI. Do not hand-edit the @rule-studio:spec banner below; edit
  the rule in Rule Studio instead so the spec and the code stay in sync.
*/

/* @rule-studio:spec
{
  "ruleId": "EX-PO050",
  "name": "Disallow JavaScript policies",
  "message": "JavaScript callouts are not permitted",
  "severity": 1,
  "nodeType": "Policy",
  "enabled": true,
  "templateId": "disallow-policy-type",
  "params": {
    "policyType": "Javascript"
  }
}
*/

const plugin = {
  ruleId: "EX-PO050",
  name: "Disallow JavaScript policies",
  message: "JavaScript callouts are not permitted",
  fatal: false,
  severity: 1,
  nodeType: "Policy",
  enabled: true,
};

const onPolicy = function (policy, cb) {
  let flagged = false;
  const disallowed = "Javascript";
  if (policy.getType() === disallowed) {
    policy.addMessage({
      plugin,
      message: 'Policy type "' + disallowed + '" is not allowed (policy "' + policy.getName() + '").'
    });
    flagged = true;
  }
  if (typeof cb === "function") {
    cb(null, flagged);
  }
};

module.exports = {
  plugin,
  onPolicy,
};
